import React from 'react'
import soundThings from '../soundThings'
import soundData from '../soundThings'

const RenderImasge = () => {
    return soundThings.map((obj) => {


        return (
            <li key={key}>
                {console.log(obj.image)}
                <img src={obj.image} => (obj.image) />
                
            </li>
        
        
                
                    
    
        )}
    )}
